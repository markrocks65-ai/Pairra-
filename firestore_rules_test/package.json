{
  "name": "pairra-firestore-rules-test",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Security-rule tests for PAIRRA's firestore.rules, run against the Firestore emulator.",
  "scripts": {
    "test": "firebase emulators:exec --project demo-pairra --only firestore \"node --test firestore_rules_test/rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.3.1"
  }
}
